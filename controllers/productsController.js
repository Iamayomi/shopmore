const productModel = require("../models/productModel");const appFeature = require("../utils/appFeature");exports.getAllProducts = async function (req, res, next) {     try {          const appProperties = new appFeature(req.query, productModel);          appProperties.filter().limiting().sorting().paginate();                  const products = await appProperties.searchQuery();          res.status(200).json({               status: "SUCCESS",               total: products.length,               data: {                    products: products               }          })     } catch (err) {          next(res.status(400).send(err.message))     }};